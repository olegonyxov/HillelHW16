let i=1;
const characters = 'abcdefghijklmnopqrstuvwxyz0123456789';
const charArr = characters.split("")
let newKeyArr=[]
while (i < 10){
    i++
    newIndex = Math.floor(Math.random()*charArr.length+1)
    newChar = charArr[newIndex]
    for (char1 in newKeyArr){
        
    }
    // console.log(newKeyArr)
}